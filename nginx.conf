server {
    listen 80;

    # 📦 Thư mục chứa build Flutter Web
    root /usr/share/nginx/html;
    index index.html;

    # 🌐 SPA Flutter Web - tránh lỗi 404 khi reload trang
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 🔄 Proxy API sang backend
    location /api/ {
        # ⚙️ Local/Docker/VPS, thay BASE_URL qua environment nếu cần
        # Khi dùng Docker Compose, backend container name là "backend-fwfe"
        # Khi deploy VPS thật, thay bằng IP hoặc domain thật
        proxy_pass http://backend-fwfe:8080/;

        # Giữ header client
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeout tăng để tránh lỗi request lâu
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;

        # CORS nếu cần
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods GET, POST, PUT, DELETE, OPTIONS;
        add_header Access-Control-Allow-Headers Authorization,Content-Type;
    }

    # ⚡ Tùy chọn cho WebSocket (nếu dùng WS)
    location /ws/ {
        proxy_pass http://backend-fwfe:8080/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }
}
